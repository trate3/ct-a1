"""Protocol layer: wire messages, digests, and the attestation interface.

Both trust sides import this module — it *is* the protocol specification, so it
is deliberately the one place code is shared across the trust boundary.
"""
